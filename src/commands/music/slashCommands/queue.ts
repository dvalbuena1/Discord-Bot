import { RESTPostAPIApplicationCommandsJSONBody } from "discord-api-types";

const data: RESTPostAPIApplicationCommandsJSONBody = {
  name: "queue",
  description: "Displays the current queue of the tracks",
};
export default data;
